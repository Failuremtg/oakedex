rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Admin card images: anyone can read (all users/devices see images), only signed-in can upload/delete
    match /cardImages/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
