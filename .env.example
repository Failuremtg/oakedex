# Firebase – copy to .env and fill with your Firebase project config.
# Get these from Firebase Console → Project settings → Your apps → Add app (Web).
# Rename this file to .env (do not commit .env).
#
# For Google and Apple sign-in (web): enable them in Firebase Console:
#   Authentication → Sign-in method → enable "Google" and "Apple".
# For Google sign-in on the app (iOS/Android): add your Firebase Web client ID below.
# Find it in Firebase Console → Project settings → Your apps → Web app → Web API Key / Client ID.
# Then in Google Cloud Console → APIs & Services → Credentials → your OAuth 2.0 Client (Web) →
# Authorized redirect URIs: add BOTH:
#   - For Expo Go: https://auth.expo.io/@YOUR_EXPO_USERNAME/oakedex
#   - For dev build: the scheme your app uses (e.g. oakedex://redirect — run the app and check the error message for the exact URI).

EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=

EXPO_PUBLIC_FIREBASE_API_KEY=
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=
EXPO_PUBLIC_FIREBASE_PROJECT_ID=
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
EXPO_PUBLIC_FIREBASE_APP_ID=

# RevenueCat – for in-app subscriptions (iOS/Android).
# Get keys from app.revenuecat.com → Project → API Keys.
# iOS key format: appl_xxxxx  (or test_xxxxx for sandbox/test projects)
# Android key format: goog_xxxxx
# Entitlement ID used in code: oakedex_pro  ← must match your RC dashboard
EXPO_PUBLIC_REVENUECAT_APPLE_API_KEY=
EXPO_PUBLIC_REVENUECAT_GOOGLE_API_KEY=

# Optional: Pokémon TCG API (pokemontcg.io) – used as fallback when TCGdex has no image or image fails.
# Free key at https://dev.pokemontcg.io/ gives 20,000 requests/day (without key: 1,000/day).
EXPO_PUBLIC_POKEMON_TCG_API_KEY=
