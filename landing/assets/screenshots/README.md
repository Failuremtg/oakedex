# Screenshots for landing page

Copy your app screenshots into this folder with these names so the landing page can display them:

| Copy from (Cursor assets) | Save as |
|---------------------------|---------|
| `..._images_binder-none-english-...png` (Collections / swipe screen) | `collections.png` |
| `..._images_collection-screen-...png` (Master collection grid) | `collection-master.png` |
| `..._images_binder-all-of-one-...png` (Single Pokémon, e.g. Voltorb) | `single-pokemon.png` |
| `..._images_binder-all-...png` (Collect Them All view) | `binder-empty.png` |
| `..._images_card-dex-...png` (Card Dex search) | `card-dex.png` |
| `..._images_profile-overview-...png` (Profile) | `profile.png` |

The logo is loaded from `../assets/images/oakedex-logo.svg` (project root). Open `index.html` from the **oakedex** project root (e.g. with a simple HTTP server) so that paths resolve correctly.
