/**
 * Firebase config for landing page signups (stored in Firestore).
 *
 * 1. Copy this file to firebase-config.js (do not commit firebase-config.js if it contains secrets).
 * 2. Get your config from Firebase Console → Project settings → Your apps → Web app.
 * 3. Fill in the values below. Same config as your Expo app (EXPO_PUBLIC_FIREBASE_*).
 */
window.FIREBASE_CONFIG = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: ''
};
